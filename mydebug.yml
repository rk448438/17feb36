---
- hosts: 172.31.47.96
  become: yes
  tasks: 
  - name: i want to execute linux command
    command: hostname -f
    register: myresult

  - name: i want to display my last result
    debug: 
      msg: "{{myresult}}"
      
            
     